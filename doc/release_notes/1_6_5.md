### 1.6.5

#### Security fixes

* The RFC 7592 client configuration update endpoint (`PUT` on the registration client URI)
  performed a database `UPDATE` without scoping to the client being updated, so a single
  authenticated update rewrote every registered OAuth application's row. It is now scoped by
  `client_id`, matching the `DELETE` handler.
